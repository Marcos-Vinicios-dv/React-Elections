const HeaderElections = ({ children }) => {
  return (
    <div className="flex flex-col flex-wrap items-center w-full justify-center">
      {children}
    </div>
  );
};

export default HeaderElections;
